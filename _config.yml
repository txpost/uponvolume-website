name:                Trevor
title:               Up on Volume
author:              Trevor
twitter:             "@UpOnVolume"
domain:              uponvolume.com
url:                 https://uponvolume.com
include:             ['_pages']
metadescription:     "Trader notes and writing by Trevor."

# baseurl is empty when the site is served at the apex domain (uponvolume.com).
baseurl:             ''

# Set to `true` to open non-internal links in new tabs, or
# set to `false` to open non-internal links in current tab.
open_external_links_in_new_tab: true

permalink:           /:slug
relative_permalinks: false

plugins:
  - jekyll-redirect-from
  - jekyll-sitemap

sass:
  sass_dir:          _sass
  style:             :compressed

collections:
  notes:
    output: true
    permalink: /:slug/

defaults:
  - scope:
      path: ""
    values:
      layout: "default"
  - scope:
      path: ""
      type: "notes"
    values:
      layout: "note"

tag_page_layout: tag
tag_page_dir: topics

# Ignore files Jekyll shouldn't ship to _site
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - vendor
  - .github
  - node_modules
